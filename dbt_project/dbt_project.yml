# Placeholder for dbt_project.yml
# Initialize with: dbt init dbt_project

name: 'nyc_tlc_analysis'
version: '1.0.0'
config-version: 2

profile: 'nyc_tlc_analysis'
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
data-paths: ["data"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"
